## Contributing

1. Install `uv` toolchain

2. `uv` install dependencies

```bash
uv sync
```

3. Running local server

```bash
uv run mkdocs serve
```

4. If you want to test local package, run the build command will generate `site` dir.

```bash
uv run mkdocs build
```
